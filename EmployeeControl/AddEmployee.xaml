<Window x:Class="EmployeeControl.AddEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmployeeControl"
        mc:Ignorable="d"
        Title="AddEmployee" Height="447" Width="551">
    <Grid>
        <Button Content="Добавить" HorizontalAlignment="Right" Margin="0,0,117,10" Height="32" VerticalAlignment="Bottom"/>
        <Button x:Name="CancelButton" Content="Отмена" Margin="0,0,26,10" Click="CancelButton_Click" HorizontalAlignment="Right" Width="82" Height="32" VerticalAlignment="Bottom"/>
        <GroupBox Margin="10,10,0,0" HorizontalAlignment="Left" Width="290" Height="129" VerticalAlignment="Top">
            <UniformGrid Rows="3" Columns="2">
                <Label Content="Фамилия" ></Label>
                <TextBox></TextBox>
                <Label Content="Имя"></Label>
                <TextBox></TextBox>
                <Label Content="Отчество"></Label>
                <TextBox></TextBox>
            </UniformGrid>
        </GroupBox>

    </Grid>
</Window>
